export type Size = "sm" | "md" | "lg";
export type XSize = "xl" | "2xl";

export enum SizeButtons {
  sm = "px-2 py-1 gap-1 rounded-lg",
  md = "px-4 py-2 gap-2 rounded-xl",
  lg = "px-6 py-3 gap-3 rounded-2xl",
}

export enum SizeTexts {
  sm = "text-sm",
  md = "text-base",
  lg = "text-lg",
  xl = "text-xl",
  "2xl" = "text-2xl",
}

export enum SizeInputs {
  sm = "text-sm h-8",
  md = "text-base h-10",
  lg = "text-lg h-12",
}
